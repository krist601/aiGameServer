version: '3.8'

services:
  mongodb:
    image: mongo:7.0
    container_name: ai-game-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password
      MONGO_INITDB_DATABASE: ai-game
    volumes:
      - mongodb_data:/data/db
      - mongodb_config:/data/configdb
    networks:
      - ai-game-network

volumes:
  mongodb_data:
  mongodb_config:

networks:
  ai-game-network:
    driver: bridge

